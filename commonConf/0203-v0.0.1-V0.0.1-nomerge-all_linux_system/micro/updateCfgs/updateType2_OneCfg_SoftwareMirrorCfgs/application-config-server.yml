server: 
  port: 7010
  
spring:
  application:
    name: config-service
  profiles:
    active: native
    
  cloud:
    config:
      server:
        native:
                search-locations: nmsCfgFolderPath

logging:
  config: log4j2-spring.xml
